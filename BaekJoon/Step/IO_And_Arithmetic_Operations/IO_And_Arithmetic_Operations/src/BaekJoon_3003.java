import java.util.Scanner;

public class BaekJoon_3003 {

	public static void main(String[] args) {
		Scanner scan = new Scanner(System.in);
		
		int k = 1 - (scan.nextInt());
		int q = 1 - (scan.nextInt());
		int l = 2 - (scan.nextInt());
		int bs = 2 - (scan.nextInt());
		int n = 2 - (scan.nextInt());
		int p = 8 - (scan.nextInt());
		
		System.out.println(k + " " + q + " " + l + " " + bs + " " + n + " " + p);

	}

}
